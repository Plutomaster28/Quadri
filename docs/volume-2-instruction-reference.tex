\documentclass[10pt]{article}
\usepackage[margin=0.8in,includefoot]{geometry}
\usepackage{fancyhdr}
\usepackage{hyperref}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{titlesec}
\usepackage{tocloft}
\pagestyle{fancy}\fancyhf{}
\fancyhead[L]{\sffamily SeaBird Volume 2 -- Instruction Reference}
\fancyhead[R]{\sffamily Architecture 3.2 / SDK 1.0}
\fancyfoot[C]{\thepage}
\setlength{\headheight}{14pt}
\titleformat{\section}{\Large\bfseries\sffamily}{\thesection}{0.7em}{}
\titleformat{\subsection}{\normalsize\bfseries\sffamily}{}{0pt}{}
\cftsetpnumwidth{2.5em}
\setlength{\cftsecnumwidth}{3.5em}
\lstset{basicstyle=\ttfamily\small,breaklines=true,frame=single,backgroundcolor=\color{gray!8}}
\hypersetup{colorlinks=true,linkcolor=black,urlcolor=black}
\sloppy\emergencystretch=1em
\begin{document}
\begin{titlepage}\centering\vspace*{2cm}
{\Huge\bfseries SeaBird ISA\\[0.3cm]}{\LARGE Volume 2: Instruction Reference\\[0.4cm]}
{\Large Architecture 3.2 / SDK 1.0}\vfill
{\large Generated from the machine-readable architectural database\\August 14, 2026}\vfill
\end{titlepage}
\tableofcontents\clearpage
\section*{Status Legend}
NORMATIVE entries have complete machine-readable contracts. RESERVED entries are retired allocations that always raise INVALID\_OP and cannot be reused in v3. PROVISIONAL entries require further definition. ALIAS entries emit the canonical instruction.\clearpage
\section{ABS -- ABS Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{34}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uABS Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- absolute_value(signed(src)); signed_min remains signed_min and sets OF
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ACCUMULATE -- ACCUMULATE Rdst, Ra}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 11}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra.

accumulate with carry chaining.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(dst + a); CF receives carry out
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ADC -- ADC Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D8}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

Add source and carry flag to destination.

\subsection*{Operation}
\begin{lstlisting}
carry_in <- CF
sum <- unsigned(dst) + unsigned(src) + carry_in
dst <- low_width_bits(sum)
update arithmetic FLAGS including carry_out
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ADD -- ADD Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{20}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uADD Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst + src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ADDI -- ADDI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{21}, I-type (1/2/4/8B)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uADD Rdst,\#imm.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst + sign_extend(immediate), width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ADDS -- ADDS Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{30}, R-type (saturate)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uADD.SAT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- clamp_signed(signed(dst) + signed(src), width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ADDU -- ADDU Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{31}, R-type (sat uns)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uADD.USAT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- min(unsigned(dst) + unsigned(src), unsigned_max(width))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AEAD\_FINAL -- AEAD\_FINAL Vd, tag}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 12}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

AEAD finalize and tag compute.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AEAD\_INIT -- AEAD\_INIT Vd, key, nonce}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 11}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

AEAD initialization (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AESDEC -- AESDEC Vd, Va, roundkey}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 01}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = roundkey.

AES inverse round helper.

\subsection*{Operation}
\begin{lstlisting}
dst <- InvMixColumns(InvShiftRows(InvSubBytes(a))) XOR roundkey
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AESENC -- AESENC Vd, Va, roundkey}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 00}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = roundkey.

AES round helper.

\subsection*{Operation}
\begin{lstlisting}
dst <- MixColumns(ShiftRows(SubBytes(a))) XOR roundkey, using AES column-major byte order
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AESGCM\_DEC -- AESGCM\_DEC Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 14}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

AES-GCM decrypt helper (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AESGCM\_ENC -- AESGCM\_ENC Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 13}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

AES-GCM encrypt helper (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AESIMC -- AESIMC Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 02}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

AES inv-mix-columns helper.

\subsection*{Operation}
\begin{lstlisting}
dst <- InvMixColumns(a)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AESKEYGEN\_ASSIST -- AESKEYGEN\_ASSIST Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 03}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

key schedule assist.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{AND -- AND Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{40}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uAND Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst & src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ANDN -- ANDN Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{CD}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uANDN Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- bitwise_not(dst) & src
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ATADD -- ATADD Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B3}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

atomic fetch-and-add.

\subsection*{Operation}
\begin{lstlisting}
old <- atomic_fetch_add(memory, src)
dst <- old
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{ATAND -- ATAND Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B5}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

atomic fetch-and-and.

\subsection*{Operation}
\begin{lstlisting}
old <- atomic_fetch_and(memory, src)
dst <- old
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{ATOR -- ATOR Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B6}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

atomic fetch-and-or.

\subsection*{Operation}
\begin{lstlisting}
old <- atomic_fetch_or(memory, src)
dst <- old
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{ATSUB -- ATSUB Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B4}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

atomic fetch-and-sub.

\subsection*{Operation}
\begin{lstlisting}
old <- atomic_fetch_sub(memory, src)
dst <- old
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{ATXOR -- ATXOR Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B7}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

atomic fetch-and-xor.

\subsection*{Operation}
\begin{lstlisting}
old <- atomic_fetch_xor(memory, src)
dst <- old
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{BCLR -- BCLR Rdst, bit}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{4D}, R-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = bit.

uBCLR Rdst,bit.

\subsection*{Operation}
\begin{lstlisting}
index <- bit mod width
dst[index] <- 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BEXTR -- BEXTR Rdst, Rsrc, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C5}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; immediate field = imm.

uBEXTR Rdst,Rsrc,imm.

\subsection*{Operation}
\begin{lstlisting}
start <- imm[7:0]; length <- imm[15:8]
dst <- zero_extend(src[start +: min(length, width-start)])
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BINSERT -- BINSERT Rdst, Rsrc, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C6}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; immediate field = imm.

uBINSERT Rdst,Rsrc,imm.

\subsection*{Operation}
\begin{lstlisting}
start <- imm[7:0]; length <- imm[15:8]
dst[start +: length] <- low_bits(src, length)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BITREV -- BITREV Rdst, Ra, bits}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 0B}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; immediate field = bits.

bit-reversal for FFT indexing.

\subsection*{Operation}
\begin{lstlisting}
dst low bits <- reverse_bit_order(a low 'bits' bits); higher bits zero
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BLSI -- BLSI Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C7}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uBLSI Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- src & (0 - src)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BLSMSK -- BLSMSK Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C8}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uBLSMSK Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- src ^ (src - 1)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BLSR -- BLSR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C9}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uBLSR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- src & (src - 1)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BRR -- BRR reg}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{6F}, B-type (ind)\\
\textbf{Operand binding:} ModR/M.r/m = reg; ModR/M.reg = 0.

uJMP [reg].

\subsection*{Operation}
\begin{lstlisting}
IP <- low_address_bits(reg)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BSET -- BSET Rdst, bit}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{4C}, R-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = bit.

uBSET Rdst,bit.

\subsection*{Operation}
\begin{lstlisting}
index <- bit mod width
dst[index] <- 1
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BTOG -- BTOG Rdst, bit}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{4E}, R-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = bit.

uBTOG Rdst,bit.

\subsection*{Operation}
\begin{lstlisting}
index <- bit mod width
dst[index] <- not dst[index]
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BTST -- BTST Rdst, bit}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{4F}, R-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = bit.

uBTST Rdst,bit.

\subsection*{Operation}
\begin{lstlisting}
index <- bit mod width
ZF <- not dst[index]; all other FLAGS unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{BZHI -- BZHI Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{CE}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uBZHI Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
index <- min(unsigned(src), width)
dst <- dst & ((1 << index) - 1)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CALL -- CALL label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{5E}, B-type (rel)\\
\textbf{Operand binding:} immediate field = label.

uPUSH ret\_addr; uJMP PC+offset.

\subsection*{Operation}
\begin{lstlisting}
SP <- SP - pointer_bytes
memory[SP, pointer_bytes] <- next_IP
if CR4.WINDOW_ENABLE then advance one logical register window
IP <- next_IP + sign_extend(relative_displacement)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{CALLA -- CALLA addr}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{5F}, B-type (abs)\\
\textbf{Operand binding:} ModR/M.r/m = addr.

uPUSH ret\_addr; uJMP addr.

\subsection*{Operation}
\begin{lstlisting}
target <- absolute_target before any window transition
SP <- SP - pointer_bytes
memory[SP, pointer_bytes] <- next_IP
if CR4.WINDOW_ENABLE then advance one logical register window
IP <- target
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{CLAMP -- CLAMP Rdst, Ra, Rmin, Rmax}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 10}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rmin; XOP1 = Rmax.

clamp between register-provided bounds.

\subsection*{Operation}
\begin{lstlisting}
dst <- min(max(signed(a), signed(min)), signed(max))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CLI -- CLI}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{85}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uCLEAR\_IF (disable interrupts).

\subsection*{Operation}
\begin{lstlisting}
IF <- 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{CLMUL\_RED -- CLMUL\_RED Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 17}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Carry-less multiply reduction helper.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CLZ -- CLZ Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{35}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uCLZ Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- count of consecutive zero bits from src[width-1] toward bit 0; zero input returns width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CLZ\_FAST -- CLZ\_FAST Rdst, Ra}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 1A}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra.

fast count leading zeros (optimized DSP).

\subsection*{Operation}
\begin{lstlisting}
dst <- count_leading_zeros(a, width); architecturally identical to CLZ
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CMP -- CMP Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{52}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Ra; ModR/M.r/m = Rb.

uCMP Ra,Rb (set flags).

\subsection*{Operation}
\begin{lstlisting}
result <- trunc_width(a - b, width); discard result
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CMPI -- CMPI Ra, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{53}, I-type\\
\textbf{Operand binding:} ModR/M.r/m = Ra; ModR/M.reg = 0; immediate field = \#imm.

uCMP Ra,\#imm (set flags).

\subsection*{Operation}
\begin{lstlisting}
result <- trunc_width(a - sign_extend(imm), width); discard result
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CMPLX\_MUL -- CMPLX\_MUL Rdst\_hi, Rdst\_lo, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 0A}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst\_hi; ModR/M.r/m = Rdst\_lo; XOP0 = Ra; XOP1 = Rb.

complex multiply (microcoded).

\subsection*{Operation}
\begin{lstlisting}
(dst_real,dst_imag) <- (a_real*b_real-a_imag*b_imag, a_real*b_imag+a_imag*b_real)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CMPS -- CMPS Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{54}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Ra; ModR/M.r/m = Rb.

uCMP.S Ra,Rb (signed).

\subsection*{Operation}
\begin{lstlisting}
result <- signed_compare(a, b, width); update FLAGS only
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CMPU -- CMPU Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{55}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Ra; ModR/M.r/m = Rb.

uCMP.U Ra,Rb (unsigned).

\subsection*{Operation}
\begin{lstlisting}
result <- unsigned_compare(a, b, width); update FLAGS only
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CMPXCHG -- CMPXCHG Rexpected, Rdesired, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B2}, M-type + XOP\\
\textbf{Operand binding:} ModR/M.reg = Rexpected; ModR/M.r/m = [addr]; XOP0 = Rdesired.

full compare-and-swap (RMW).

\subsection*{Operation}
\begin{lstlisting}
address <- effective_address(mem)
old <- atomic_load(memory[address, operand_bytes])
if old == Rexpected: atomic_store(memory[address, operand_bytes], Rdesired); ZF <- 1
else: Rexpected <- old; ZF <- 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=defined, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{CONV\_STEP -- CONV\_STEP dst, a, b}
\textbf{Status:} RESERVED\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 0C}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

convolution multiply-accumulate step.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CPYB -- CPYB dst, src, len}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{7B}, X-type\\
\textbf{Operand binding:} ModR/M.reg = dst; ModR/M.r/m = src; XOP0 = len.

Byte copy loop (microcoded).

\subsection*{Operation}
\begin{lstlisting}
copy len bytes from src to dst as if by increasing-address temporary-buffer semantics
on fault, completed-byte count is written to R0 and remaining addresses to R1/R2
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Restartable sequence of ordinary TSO accesses in increasing element order; completed stores remain visible if a later element faults.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{CPYW -- CPYW dst, src, len}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{7C}, X-type\\
\textbf{Operand binding:} ModR/M.reg = dst; ModR/M.r/m = src; XOP0 = len.

Word copy loop (microcoded).

\subsection*{Operation}
\begin{lstlisting}
copy len operand-width words from src to dst with temporary-buffer overlap semantics
on fault, completed-word count is written to R0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Restartable sequence of ordinary TSO accesses in increasing element order; completed stores remain visible if a later element faults.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{CTR\_XOR -- CTR\_XOR Vd, Va, counter}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 10}, V-type+M\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Counter-mode XOR block.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{CTZ -- CTZ Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{36}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uCTZ Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- count of consecutive zero bits from src[0] toward bit width-1; zero input returns width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{DEC -- DEC Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{2C}, R-type (unary)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uDEC Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(dst - 1, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{DIV -- DIV Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{26}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uDIV Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
if src == 0: raise DIV_ZERO
dst <- signed(dst) / signed(src), rounded toward zero
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\end{itemize}
\clearpage

\section{DIVI -- DIVI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{27}, I-type (1/2/4/8B)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uDIV Rdst,\#imm.

\subsection*{Operation}
\begin{lstlisting}
divisor <- sign_extend(immediate)
if divisor == 0: raise DIV_ZERO
dst <- signed(dst) / divisor, rounded toward zero
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\end{itemize}
\clearpage

\section{DOTP -- DOTP Rdst, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 12}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Va; XOP0 = Vb.

dot-product across vector lanes.

\subsection*{Operation}
\begin{lstlisting}
dst <- left-to-right sum of signed products of corresponding active vector lanes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ENDBR -- ENDBR}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 12}, IBT\\
\textbf{Operand binding:} No ModR/M or XOP byte.

Mark a legal indirect branch target.

\subsection*{Operation}
\begin{lstlisting}
architectural state unchanged; instruction is a valid IBT landing marker
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ENTER -- ENTER \#framesize}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{91}, X-type\\
\textbf{Operand binding:} immediate field = \#framesize.

uPUSH BP; uMOV BP,SP; uSUB SP,size.

\subsection*{Operation}
\begin{lstlisting}
frame_size <- zero_extend(immediate) rounded to stack alignment
push old R6
R6 <- SP
SP <- SP - frame_size
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{EOI -- EOI vector}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 0D}, SMP\\
\textbf{Operand binding:} ModR/M.r/m = vector; ModR/M.reg = 0.

Complete a local in-service interrupt.

\subsection*{Operation}
\begin{lstlisting}
remove vector from the local in-service set and permit lower-priority delivery
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{EXT -- EXT Rdst, Rsrc, range}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{51}, R-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; XOP0 = range.

uEXT Rdst,Rsrc,range.

\subsection*{Operation}
\begin{lstlisting}
validate start + length <= width
dst <- zero_extend(src[start +: length])
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{FABS -- FABS Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{AF}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

fABS Fd,Fa (absolute value).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(clear_sign(a), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FADD -- FADD Fd, Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A6}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb.

fADD Fd,Fa,Fb (IEEE 754).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(a + b, destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCHS -- FCHS Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 0E}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Change sign (alternate to FNEG).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(copy_sign(a, not sign(a)), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCLASS -- FCLASS Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 0D}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Classify floating value.

\subsection*{Operation}
\begin{lstlisting}
dst <- classification bitmap: bit0 -inf, bit1 negative finite, bit2 -zero, bit3 +zero, bit4 positive finite, bit5 +inf, bit6 sNaN, bit7 qNaN
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCMP -- FCMP Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{AB}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fa; ModR/M.r/m = Fb.

fCMP Fa,Fb (set FP flags).

\subsection*{Operation}
\begin{lstlisting}
classify operands
unordered: PF=1, ZF=1, CF=1; less: CF=1; equal: ZF=1; greater: all three clear
clear OF, SF, AF
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVT.D2S -- FCVT.D2S Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 0B}, R-type (conv)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Convert double to single precision.

\subsection*{Operation}
\begin{lstlisting}
convert binary64 source to binary32 using FPCR rounding
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVT.S2D -- FCVT.S2D Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 0A}, R-type (conv)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Convert single to double precision.

\subsection*{Operation}
\begin{lstlisting}
convert binary32 source to exact binary64 destination
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVTI -- FCVTI Fd, Ra}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{AC}, R-type (conv)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Ra.

Convert integer Ra to float Fd.

\subsection*{Operation}
\begin{lstlisting}
convert signed integer source to destination IEEE format using FPCR rounding
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVTINT -- FCVTINT Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 0C}, R-type (conv)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Float format conversion (mixed).

\subsection*{Operation}
\begin{lstlisting}
convert between the source and encoded destination IEEE formats using FPCR rounding
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVTS -- FCVTS Ra, Fd}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{AD}, R-type (conv)\\
\textbf{Operand binding:} ModR/M.reg = Ra; ModR/M.r/m = Fd.

Convert float Fd to integer Ra.

\subsection*{Operation}
\begin{lstlisting}
convert IEEE source to signed integer by truncation toward zero; invalid result raises or returns signed_min when masked
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVTU -- FCVTU Fd, Ra}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{DB}, R-type (unsigned conversion)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Ra; VectorCtl selects the scalar IEEE format.

Convert an unsigned integer to a scalar IEEE value.

\subsection*{Operation}
\begin{lstlisting}
convert unsigned integer source to destination IEEE format using FPCR rounding
update FPSR; raise an unmasked FPU_ERROR before writeback
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FCVTUS -- FCVTUS Ra, Fs}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{DC}, R-type (unsigned conversion)\\
\textbf{Operand binding:} ModR/M.reg = Ra; ModR/M.r/m = Fs; VectorCtl selects the scalar IEEE format.

Convert a scalar IEEE value to an unsigned integer.

\subsection*{Operation}
\begin{lstlisting}
convert IEEE source to unsigned integer by truncation toward zero
invalid result raises FPU_ERROR or returns zero when masked
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FDIV -- FDIV Fd, Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A9}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb.

fDIV Fd,Fa,Fb (IEEE 754).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(a / b, destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FENCE -- FENCE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{BA}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

full memory fence (serializing).

\subsection*{Operation}
\begin{lstlisting}
execute MFENCE semantics
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{FFT\_STAGE -- FFT\_STAGE dst, src, stage}
\textbf{Status:} RESERVED\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 0D}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

FFT stage helper (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{FIR\_STEP -- FIR\_STEP dst, coef, sample}
\textbf{Status:} RESERVED\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 08}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

single FIR tap (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{FIXED\_ADD -- FIXED\_ADD Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 07}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

fixed-point add.

\subsection*{Operation}
\begin{lstlisting}
dst <- saturate_signed(a + b, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{FIXED\_MUL -- FIXED\_MUL Rdst, Ra, Rb, Rq}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 06}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb; XOP1 = Rq.

fixed-point multiply with Q-format selected by Rq.

\subsection*{Operation}
\begin{lstlisting}
dst <- round_to_nearest_even((signed(a) * signed(b)) / 2^Q), saturated to width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{FLD -- FLD Fd, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 10}, M-type (scalar FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = [addr]. Operand-width prefix selects 16/32/64/128-bit scalar format.

Load one scalar floating-point value without accessing adjacent bytes.

\subsection*{Operation}
\begin{lstlisting}
read exactly operand_width bits from memory
Fd.low <- loaded bit pattern
clear destination bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary scalar load.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item ALIGN\_CHECK, PAGE\_FAULT, or GPF before architectural modification
\end{itemize}
\clearpage

\section{FLUSH -- FLUSH [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{73}, M-type\\
\textbf{Operand binding:} ModR/M.r/m = [addr].

uFLUSH [addr] (cache writeback).

\subsection*{Operation}
\begin{lstlisting}
write back dirty data for the addressed cache line to the point of coherency and invalidate the local copy
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Cache-maintenance operation ordered after older accesses to the addressed line; use the required fence or ISYNC before relying on global visibility or refetch.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{FMADD -- FMADD Fd, Fa, Fb, Fc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 00}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb; XOP1 = Fc.

Fused multiply-add (Fd = Fa * Fb + Fc).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(fused(a * b + c), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FMAX -- FMAX Fd, Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 05}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb.

Floating-point max.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(maximumNumber(a, b), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FMIN -- FMIN Fd, Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 04}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb.

Floating-point min.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(minimumNumber(a, b), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FMSUB -- FMSUB Fd, Fa, Fb, Fc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 01}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb; XOP1 = Fc.

Fused multiply-subtract.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(fused(a * b - c), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FMUL -- FMUL Fd, Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A8}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb.

fMUL Fd,Fa,Fb (IEEE 754).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(a * b, destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FNEG -- FNEG Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{AE}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

fNEG Fd,Fa (negate).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(copy_sign(a, not sign(a)), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FNMADD -- FNMADD Fd, Fa, Fb, Fc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 02}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb; XOP1 = Fc.

Fused neg-multiply-add.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(fused(-(a * b) + c), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FNMSUB -- FNMSUB Fd, Fa, Fb, Fc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 03}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb; XOP1 = Fc.

Fused neg-multiply-subtract.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(fused(-(a * b) - c), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FRECIP -- FRECIP Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 06}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Reciprocal estimate / refine.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(1 / a, destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FRND -- FRND Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 08}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Round using current rounding mode.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(round_integral(a, FPCR.rounding_mode), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FRNDZ -- FRNDZ Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 09}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Round toward zero.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(round_integral(a, toward_zero), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FRSQRT -- FRSQRT Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 07}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Reciprocal sqrt estimate / refine.

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(1 / square_root(a), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FSQRT -- FSQRT Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{AA}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

fSQRT Fd,Fa (square root).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(square_root(a), destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FST -- FST [addr], Fs}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 11}, M-type (scalar FP)\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Fs. Operand-width prefix selects 16/32/64/128-bit scalar format.

Store one scalar floating-point value without accessing adjacent bytes.

\subsection*{Operation}
\begin{lstlisting}
write exactly operand_width low bits of Fs to memory as a little-endian scalar
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary scalar store.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item ALIGN\_CHECK, PAGE\_FAULT, or GPF before architectural modification
\end{itemize}
\clearpage

\section{FSUB -- FSUB Fd, Fa, Fb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A7}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa; XOP0 = Fb.

fSUB Fd,Fa,Fb (IEEE 754).

\subsection*{Operation}
\begin{lstlisting}
result <- correctly_rounded_IEEE754(a - b, destination_format, FPCR)
update FPSR sticky exception bits
if an unmasked exception exists: raise FPU_ERROR before writeback
destination <- result; clear bits above scalar width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{FTEST -- FTEST Fd, Fa}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} FPX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 05 0F}, R-type (FP)\\
\textbf{Operand binding:} ModR/M.reg = Fd; ModR/M.r/m = Fa.

Test predicates and exceptions.

\subsection*{Operation}
\begin{lstlisting}
dst <- FPSR exception bitmap; architectural FP state is otherwise unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{GCM\_DEC\_BLOCK -- GCM\_DEC\_BLOCK Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 07}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

GCM decryption block helper.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{GCM\_ENC\_BLOCK -- GCM\_ENC\_BLOCK Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 06}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

GCM encryption block helper.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{GETCPL -- GETCPL Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 19}, BASE\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

Read current privilege level.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(CPL)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{GETPID -- GETPID Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{8B}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uGET\_PROCESS\_ID Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(PID)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{GETTID -- GETTID Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{8C}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uGET\_THREAD\_ID Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(TID)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{GHASH -- GHASH Vd, Va, H}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 05}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = H.

GCM GHASH polynomial multiply accumulate.

\subsection*{Operation}
\begin{lstlisting}
dst <- gf128_multiply(dst_old XOR a, H) modulo x^128+x^7+x^2+x+1, bit-reflected GCM order
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{HLT -- HLT}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{7E}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uHALT (stop execution).

\subsection*{Operation}
\begin{lstlisting}
enter halted state until RESET, NMI, INIT, or an enabled interrupt
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{IIR\_STEP -- IIR\_STEP dst, coef, state}
\textbf{Status:} RESERVED\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 09}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

single IIR step helper.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{IN -- IN Rdst, port}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 01}, PIO\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = port.

Read 8/16/32-bit programmed-I/O port.

\subsection*{Operation}
\begin{lstlisting}
validate IOPL or task I/O permission
dst <- zero_extend(io_space[port, operand_bytes])
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{INC -- INC Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{2B}, R-type (unary)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uINC Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(dst + 1, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{INVDC -- INVDC [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{75}, M-type\\
\textbf{Operand binding:} ModR/M.r/m = [addr].

uINVAL\_DCACHE [addr].

\subsection*{Operation}
\begin{lstlisting}
invalidate a clean local data-cache line; raise GPF if dirty data would be discarded
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Cache-maintenance operation ordered after older accesses to the addressed line; use the required fence or ISYNC before relying on global visibility or refetch.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{INVIC -- INVIC [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{74}, M-type\\
\textbf{Operand binding:} ModR/M.r/m = [addr].

uINVAL\_ICACHE [addr].

\subsection*{Operation}
\begin{lstlisting}
invalidate the local instruction-cache line containing the address; requires subsequent ISYNC before execution
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Cache-maintenance operation ordered after older accesses to the addressed line; use the required fence or ISYNC before relying on global visibility or refetch.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{INVTLB -- INVTLB va, asid}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 09}, MMU\\
\textbf{Operand binding:} ModR/M.reg = va; ModR/M.r/m = asid.

Invalidate one local translation.

\subsection*{Operation}
\begin{lstlisting}
invalidate local translation matching (va, asid) after older page-table stores are visible
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{INVTLBALL -- INVTLBALL}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 0B}, MMU\\
\textbf{Operand binding:} No ModR/M or XOP byte.

Invalidate all local translations.

\subsection*{Operation}
\begin{lstlisting}
invalidate every local translation including global entries
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{INVTLBASID -- INVTLBASID asid}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 0A}, MMU\\
\textbf{Operand binding:} ModR/M.r/m = asid; ModR/M.reg = 0.

Invalidate one local address space.

\subsection*{Operation}
\begin{lstlisting}
invalidate all local non-global translations matching asid
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{IRET -- IRET}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{84}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uIRET (return from interrupt).

\subsection*{Operation}
\begin{lstlisting}
validate complete interrupt frame
atomically restore SP, IP, FLAGS, and CPL
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{ISYNC -- ISYNC}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 08}, BASE\\
\textbf{Operand binding:} No ModR/M or XOP byte.

Serialize subsequent instruction fetch.

\subsection*{Operation}
\begin{lstlisting}
discard stale prefetched instructions
serialize all subsequent instruction fetch
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordering is the operation itself.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{JC -- JC label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{67}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF CF=1.

\subsection*{Operation}
\begin{lstlisting}
if CF == 1 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JE -- JE label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{61}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF ZF=1.

\subsection*{Operation}
\begin{lstlisting}
if ZF == 1 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JG -- JG label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{63}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF (SF=OF) \& ZF=0.

\subsection*{Operation}
\begin{lstlisting}
if ZF == 0 and SF == OF then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JGE -- JGE label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{64}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF SF=OF.

\subsection*{Operation}
\begin{lstlisting}
if SF == OF then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JL -- JL label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{65}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF SF!=OF.

\subsection*{Operation}
\begin{lstlisting}
if SF != OF then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JLE -- JLE label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{66}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF (SF!=OF) | ZF=1.

\subsection*{Operation}
\begin{lstlisting}
if ZF == 1 or SF != OF then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JMP -- JMP label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{5C}, B-type (rel)\\
\textbf{Operand binding:} immediate field = label.

uJMP PC+offset.

\subsection*{Operation}
\begin{lstlisting}
IP <- next_IP + sign_extend(relative_displacement)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JMPA -- JMPA addr}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{5D}, B-type (abs)\\
\textbf{Operand binding:} ModR/M.r/m = addr.

uJMP addr.

\subsection*{Operation}
\begin{lstlisting}
IP <- zero_extend(absolute_target, address_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JMPR -- JMPR}
\textbf{Status:} ALIAS\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{alias}, assembler alias\\
\textbf{Operand binding:} Inherited from canonical instruction.

Assembler alias for BRR.

\subsection*{Operation}
\begin{lstlisting}
assemble as BRR
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Inherited from canonical instruction.
\subsection*{Exceptions}
\begin{itemize}
\item Inherited from canonical instruction.
\end{itemize}
\clearpage

\section{JNC -- JNC label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{68}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF CF=0.

\subsection*{Operation}
\begin{lstlisting}
if CF == 0 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JNE -- JNE label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{62}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF ZF=0.

\subsection*{Operation}
\begin{lstlisting}
if ZF == 0 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JNO -- JNO label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{6A}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF OF=0.

\subsection*{Operation}
\begin{lstlisting}
if OF == 0 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JNS -- JNS label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{6C}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF SF=0.

\subsection*{Operation}
\begin{lstlisting}
if SF == 0 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JNZ -- JNZ}
\textbf{Status:} ALIAS\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{alias}, assembler alias\\
\textbf{Operand binding:} Inherited from canonical instruction.

Assembler alias for JNE.

\subsection*{Operation}
\begin{lstlisting}
assemble as JNE
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Inherited from canonical instruction.
\subsection*{Exceptions}
\begin{itemize}
\item Inherited from canonical instruction.
\end{itemize}
\clearpage

\section{JNZR -- JNZR reg, label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{6E}, B-type (reg)\\
\textbf{Operand binding:} ModR/M.r/m = reg; ModR/M.reg = 0; immediate field = label.

uBRANCH\_IF reg!=0.

\subsection*{Operation}
\begin{lstlisting}
if reg != 0 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JO -- JO label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{69}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF OF=1.

\subsection*{Operation}
\begin{lstlisting}
if OF == 1 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JS -- JS label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{6B}, B-type (cond)\\
\textbf{Operand binding:} immediate field = label.

uBRANCH\_IF SF=1.

\subsection*{Operation}
\begin{lstlisting}
if SF == 1 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{JZ -- JZ}
\textbf{Status:} ALIAS\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{alias}, assembler alias\\
\textbf{Operand binding:} Inherited from canonical instruction.

Assembler alias for JE.

\subsection*{Operation}
\begin{lstlisting}
assemble as JE
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Inherited from canonical instruction.
\subsection*{Exceptions}
\begin{itemize}
\item Inherited from canonical instruction.
\end{itemize}
\clearpage

\section{JZR -- JZR reg, label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{6D}, B-type (reg)\\
\textbf{Operand binding:} ModR/M.r/m = reg; ModR/M.reg = 0; immediate field = label.

uBRANCH\_IF reg=0.

\subsection*{Operation}
\begin{lstlisting}
if reg == 0 then IP <- next_IP + sign_extend(displacement) else IP <- next_IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{LD -- LD Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{10}, M-type + disp\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

uLOAD Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
address <- effective_address(mem)
dst <- zero_extend(memory[address, operand_bytes], architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDB -- LDB Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{13}, M-type (1B)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

uLOAD.B Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(memory[effective_address(mem), 1], architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDH -- LDH Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{14}, M-type (2B)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

uLOAD.H Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(memory[effective_address(mem), 2], architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDI -- LDI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{12}, I-type + disp\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uLOAD Rdst,[\#addr+imm].

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(memory[zero_extend(immediate_address), operand_bytes], architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDN -- LDN Rdst, [addr-]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{78}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr-].

uDEC addr; uLOAD Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
new_address <- base - operand_bytes
dst <- memory[new_address, operand_bytes]
base <- new_address after successful load
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDP -- LDP Rdst1, Rdst2, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{1B}, M-type + pair\\
\textbf{Operand binding:} ModR/M.reg = Rdst1; ModR/M.r/m = Rdst2; XOP0 = [addr].

uLOAD Rdst1,[addr]; uLOAD Rdst2,[addr+sz].

\subsection*{Operation}
\begin{lstlisting}
address <- effective_address(mem)
validate both accesses before writeback
dst1 <- memory[address, operand_bytes]
dst2 <- memory[address + operand_bytes, operand_bytes]
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDQ -- LDQ Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{16}, M-type (8B)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

uLOAD.Q Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(memory[effective_address(mem), 8], architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDW -- LDW Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{15}, M-type (4B)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

uLOAD.W Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(memory[effective_address(mem), 4], architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LDX -- LDX Rdst, [addr+]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{76}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr+].

uLOAD Rdst,[addr]; uINC addr.

\subsection*{Operation}
\begin{lstlisting}
old_address <- base
dst <- memory[old_address, operand_bytes]
base <- old_address + operand_bytes after successful load
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LEA -- LEA Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{1D}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

uADDR Rdst,[addr].

\subsection*{Operation}
\begin{lstlisting}
dst <- effective_address(mem); no memory access is performed
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LEAS -- LEAS Rdst, [base+idx*s+d]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{1E}, M-type + SIB\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [base+idx*s+d].

uMUL tmp,idx,s; uADD tmp,base,tmp; uADD Rdst,tmp,d.

\subsection*{Operation}
\begin{lstlisting}
dst <- base + index * scale + sign_extend(displacement)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LEAVE -- LEAVE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{92}, X-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uMOV SP,BP; uPOP BP.

\subsection*{Operation}
\begin{lstlisting}
SP <- R6
R6 <- memory[SP, pointer_bytes]
SP <- SP + pointer_bytes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LFENCE -- LFENCE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{BB}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

load fence (ordering loads).

\subsection*{Operation}
\begin{lstlisting}
complete all older loads before any younger load executes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LL -- LL Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B8}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

load-linked.

\subsection*{Operation}
\begin{lstlisting}
dst <- atomic_load(memory)
reservation <- (physical_address, operand_bytes, coherence_version)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LOAD -- LOAD}
\textbf{Status:} ALIAS\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{alias}, assembler alias\\
\textbf{Operand binding:} Inherited from canonical instruction.

Assembler alias for LD.

\subsection*{Operation}
\begin{lstlisting}
assemble as LD
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Inherited from canonical instruction.
\subsection*{Exceptions}
\begin{itemize}
\item Inherited from canonical instruction.
\end{itemize}
\clearpage

\section{LOADCTX -- LOADCTX [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 18}, BASE\\
\textbf{Operand binding:} ModR/M.r/m = [addr].

Validate and load a core thread context.

\subsection*{Operation}
\begin{lstlisting}
validate complete standard context image
atomically load core context; execution resumes at restored IP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LOCK -- LOCK prefix}
\textbf{Status:} RESERVED\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B0}, Prefix\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Atomicize following memory op.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LOCKNOP -- LOCKNOP}
\textbf{Status:} RESERVED\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{BE}, Prefix\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

no-op lock prefix (compat).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{LSHIFT -- LSHIFT Rdst, Ra, shamt}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 15}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; immediate field = shamt.

logical left shift.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(a << (shamt mod width))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{LZCNT -- LZCNT Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C2}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uLZCNT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- count_leading_zeros(src, width); zero input returns width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MAC32 -- MAC32 Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 00}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

Rdst += (Ra * Rb) (32-bit MAC).

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(dst + signed32(a) * signed32(b))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MAC64 -- MAC64 Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 01}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

64-bit multiply-accumulate.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(dst + signed64(a) * signed64(b))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MACS -- MACS Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 02}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

signed saturating MAC.

\subsection*{Operation}
\begin{lstlisting}
dst <- saturate_signed(dst + signed(a) * signed(b), width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MAD32 -- MAD32 Rdst, Ra, Rb, Rc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 1C}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb; XOP1 = Rc.

multiply-add with extra operand.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(signed32(a) * signed32(b) + c)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MASK -- MASK Rdst, Rsrc, mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{50}, R-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; immediate field = mask.

uMASK Rdst,Rsrc,mask.

\subsection*{Operation}
\begin{lstlisting}
dst <- src & zero_extend(mask, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MAX -- MAX Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{58}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMAX Rdst,Rsrc (max value).

\subsection*{Operation}
\begin{lstlisting}
dst <- dst if signed(dst) >= signed(src) else src
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MEMFILL -- MEMFILL dst, val, len}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{7D}, X-type\\
\textbf{Operand binding:} ModR/M.reg = dst; ModR/M.r/m = val; XOP0 = len.

Memory fill loop (microcoded).

\subsection*{Operation}
\begin{lstlisting}
for i in 0 .. len-1: memory[dst + i * operand_bytes] <- low_operand_bits(value)
on fault, completed-element count is written to R0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Restartable sequence of ordinary TSO accesses in increasing element order; completed stores remain visible if a later element faults.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{MFENCE -- MFENCE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{BD}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

mixed fence (implementation hint).

\subsection*{Operation}
\begin{lstlisting}
complete all older loads, stores, and device accesses before younger memory accesses
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{MIN -- MIN Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{59}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMIN Rdst,Rsrc (min value).

\subsection*{Operation}
\begin{lstlisting}
dst <- dst if signed(dst) <= signed(src) else src
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MLAUNCH -- MLAUNCH [params]}
\textbf{Status:} RESERVED\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 16}, MLAUNCH\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Begin measured platform launch.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{MOD -- MOD Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{28}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMOD Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
if src == 0: raise DIV_ZERO
dst <- signed(dst) remainder signed(src); remainder has dividend sign
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\end{itemize}
\clearpage

\section{MODI -- MODI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{29}, I-type (1/2/4/8B)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uMOD Rdst,\#imm.

\subsection*{Operation}
\begin{lstlisting}
divisor <- sign_extend(immediate)
if divisor == 0: raise DIV_ZERO
dst <- signed(dst) remainder divisor
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\end{itemize}
\clearpage

\section{MOV -- MOV Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{00}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMOV Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(src, architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVHI -- MOVHI Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{04}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMOVHI Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst.high_half <- src.low_half
dst.low_half <- 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVI -- MOVI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{01}, I-type (1/2/4/8B)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uMOV Rdst,\#imm.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(immediate, architectural_register_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVLO -- MOVLO Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{05}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMOVLO Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(src.low_half, destination_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVM -- MOVM Rdst, mask}
\textbf{Status:} RESERVED\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{7A}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Multi-reg masked move (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVSWP -- MOVSWP Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{06}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uBSWAP Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- reverse_bytes(src, operand_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVSX -- MOVSX Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{03}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMOV Rdst,Rsrc; uSEXT Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- sign_extend(src, destination_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MOVZX -- MOVZX Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{02}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMOV Rdst,Rsrc; uZEXT Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- zero_extend(src, destination_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MSUB -- MSUB Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 03}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

multiply-subtract.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(dst - a * b)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MUL -- MUL Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{24}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMUL Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst * src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MULH -- MULH Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{2D}, R-type (high bits)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uMULH Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
product <- signed(dst) * signed(src) at twice operand width
dst <- high_width_bits(product)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{MULI -- MULI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{25}, I-type (1/2/4/8B)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uMUL Rdst,\#imm.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst * sign_extend(immediate), width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{NAND -- NAND Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{44}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uNAND Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst NAND src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{NEG -- NEG Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{2A}, R-type (unary)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uNEG Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(0 - dst, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{NOR -- NOR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{45}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uNOR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst NOR src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{NOT -- NOT Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{43}, R-type (unary)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uNOT Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- bitwise_not(dst, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{OR -- OR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{41}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uOR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst | src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{OUT -- OUT port, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 02}, PIO\\
\textbf{Operand binding:} ModR/M.mod = 11; ModR/M.reg = 0; ModR/M.r/m = Rsrc; trailing u16le field = port.

Write 8/16/32-bit programmed-I/O port.

\subsection*{Operation}
\begin{lstlisting}
validate IOPL or task I/O permission
io_space[port, operand_bytes] <- low_bits(src)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{PACK\_SAT -- PACK\_SAT Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 0E}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

pack with saturation.

\subsection*{Operation}
\begin{lstlisting}
dst <- concatenate(saturating_narrow(a), saturating_narrow(b))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{PCLMULQDQ -- PCLMULQDQ Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 04}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

carry-less multiply (GF(2\textasciicircum{}k)).

\subsection*{Operation}
\begin{lstlisting}
dst <- carryless_product(selected_64(a), selected_64(b)); result is 128 bits
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{PDEP -- PDEP Rdst, Rsrc, Rmask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C0}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; XOP0 = Rmask.

uPDEP Rdst,Rsrc,Rmask.

\subsection*{Operation}
\begin{lstlisting}
dst <- deposit low source bits into set-bit positions of mask in increasing bit order
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{PEXT -- PEXT Rdst, Rsrc, Rmask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C1}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; XOP0 = Rmask.

uPEXT Rdst,Rsrc,Rmask.

\subsection*{Operation}
\begin{lstlisting}
dst <- pack source bits selected by mask into low bits in increasing bit order
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{POLY\_MUL -- POLY\_MUL Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 0F}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Polynomial multiply for crypto transforms.

\subsection*{Operation}
\begin{lstlisting}
dst <- carryless polynomial product of a and b truncated to destination vector width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{POP -- POP Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{8E}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uLOAD Rdst,[SP]; uINC SP.

\subsection*{Operation}
\begin{lstlisting}
value <- memory[SP, pointer_bytes]
SP <- SP + pointer_bytes
dst <- zero_extend(value)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{POPA -- POPA}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{90}, X-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

Atomically validate and restore R0--R31.

\subsection*{Operation}
\begin{lstlisting}
validate and load all 32 slots before writeback
restore R0-R31 atomically; restored R7 supplies final SP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{POPC -- POPC Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{37}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uPOPC Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- number of one bits in low width bits of src
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{POPCNT -- POPCNT Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C4}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uPOPCNT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- population_count(src, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{POPF -- POPF}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{94}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uLOAD FLAGS,[SP]; uINC SP.

\subsection*{Operation}
\begin{lstlisting}
new_flags <- memory[SP, pointer_bytes]
validate writable FLAGS bits for CPL
FLAGS <- new_flags
SP <- SP + pointer_bytes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{POPQ -- POPQ regpair}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{96}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = regpair; ModR/M.reg = 0.

Pop register pair (64-bit).

\subsection*{Operation}
\begin{lstlisting}
validate both stack slots
load named register pair
SP <- SP + 2 * pointer_bytes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{PREFETCH -- PREFETCH [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{72}, M-type\\
\textbf{Operand binding:} ModR/M.r/m = [addr].

uPREFETCH [addr] (hint).

\subsection*{Operation}
\begin{lstlisting}
request that the cache line containing effective_address(mem) be fetched; faults are suppressed and state is unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Non-binding cache hint; creates no memory-order edge and suppresses address, translation, and permission faults.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{PUSH -- PUSH Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{8D}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rsrc; ModR/M.reg = 0.

uDEC SP; uSTORE [SP],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
SP <- SP - pointer_bytes
memory[SP, pointer_bytes] <- low_bits(src, pointer_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{PUSHA -- PUSHA}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{8F}, X-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

Push pre-instruction R0--R31 state.

\subsection*{Operation}
\begin{lstlisting}
snapshot R0-R31 including pre-instruction SP
validate one 32-slot stack range
SP <- SP - 32 * pointer_bytes
store snapshot in increasing register order
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{PUSHF -- PUSHF}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{93}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uDEC SP; uSTORE [SP],FLAGS.

\subsection*{Operation}
\begin{lstlisting}
SP <- SP - pointer_bytes
memory[SP, pointer_bytes] <- FLAGS
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{PUSHQ -- PUSHQ regpair}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{95}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = regpair; ModR/M.reg = 0.

Push register pair (64-bit).

\subsection*{Operation}
\begin{lstlisting}
SP <- SP - 2 * pointer_bytes
store the named consecutive register pair in increasing register order
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{QUERY -- QUERY leaf, subleaf}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 00}, BASE\\
\textbf{Operand binding:} ModR/M.reg = leaf; ModR/M.r/m = subleaf.

Return discovery data in R0--R3.

\subsection*{Operation}
\begin{lstlisting}
(R0, R1, R2, R3) <- query_leaf(leaf, subleaf)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RDCR -- RDCR Rdst, CRn}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{80}, S-type\\
\textbf{Operand binding:} ModR/M.mod = 11; ModR/M.reg = 0; ModR/M.r/m = Rdst; OREX extends only Rdst; trailing u16le field = CRn; canonical length = 4 bytes, or 7 bytes with FE/OREX.

uREAD\_CTRL\_REG Rdst,CRn.

\subsection*{Operation}
\begin{lstlisting}
validate system-register number and read permission
dst <- zero_extend(system_register[CRn])
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RDPMC -- RDPMC Rdst, counter}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 14}, PMU\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = counter.

Read an enabled performance counter.

\subsection*{Operation}
\begin{lstlisting}
validate PMU enable and counter index
dst <- performance_counter[counter]
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RDTIME -- RDTIME Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{88}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uREAD\_TIMER Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- low_operand_width_bits(TIMER)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RDTS -- RDTS Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{89}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

uREAD\_TIMESTAMP Rdst.

\subsection*{Operation}
\begin{lstlisting}
dst <- low_operand_width_bits(TIMER)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RESET -- RESET}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{7F}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uRESET (soft reset).

\subsection*{Operation}
\begin{lstlisting}
discard current context and enter the architecturally defined reset state
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{RET -- RET}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{60}, B-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uPOP PC.

\subsection*{Operation}
\begin{lstlisting}
target <- memory[SP, pointer_bytes]
if CR4.WINDOW_ENABLE then validate and restore the previous logical register window
SP <- SP + pointer_bytes
IP <- target
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO stack access using the active architectural stack pointer.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{RNDQ -- RNDQ Rdst, Ra, Rq}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 19}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rq.

rounding to Q-format selected by Rq.

\subsection*{Operation}
\begin{lstlisting}
dst <- round_to_nearest_even(signed(a) / 2^q) * 2^q
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RNGGET -- RNGGET Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 15}, RNG\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

Return random data and success in CF.

\subsection*{Operation}
\begin{lstlisting}
request operand-width random bits
on success dst <- random_bits and CF <- 1; on temporary failure dst <- 0 and CF <- 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RNG\_GET -- RNG\_GET Rdst}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 0E}, S-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Read random value from HRNG.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RNG\_SEED -- RNG\_SEED Rdst, seed}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 02 0D}, S-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Seed hardware RNG / reseed.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{ROL -- ROL Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{4A}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uROL Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
count <- src mod width
dst <- rotate_left(dst, count, width)
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined for shifts; unchanged for rotates, AF=undefined, ZF=defined for shifts; unchanged for rotates, SF=defined for shifts; unchanged for rotates, OF=defined only for count 1.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ROR -- ROR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{4B}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uROR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
count <- src mod width
dst <- rotate_right(dst, count, width)
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined for shifts; unchanged for rotates, AF=undefined, ZF=defined for shifts; unchanged for rotates, SF=defined for shifts; unchanged for rotates, OF=defined only for count 1.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RORX -- RORX Rdst, Rsrc, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{CA}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; immediate field = imm.

uRORX Rdst,Rsrc,imm.

\subsection*{Operation}
\begin{lstlisting}
dst <- rotate_right(src, immediate mod width, width); FLAGS unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{RSHIFT\_ROUND -- RSHIFT\_ROUND Rdst, Ra, shamt}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 14}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; immediate field = shamt.

right shift with rounding.

\subsection*{Operation}
\begin{lstlisting}
dst <- round_to_nearest_even(signed(a) / 2^shamt)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SAR -- SAR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{49}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSAR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
count <- src mod width
dst <- signed(dst) >> count with sign fill
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined for shifts; unchanged for rotates, AF=undefined, ZF=defined for shifts; unchanged for rotates, SF=defined for shifts; unchanged for rotates, OF=defined only for count 1.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SATADD -- SATADD Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 05}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

saturating add.

\subsection*{Operation}
\begin{lstlisting}
dst <- saturate_signed(a + b, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SATSUB -- SATSUB Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 04}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

saturating subtract.

\subsection*{Operation}
\begin{lstlisting}
dst <- saturate_signed(a - b, width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SAVECTX -- SAVECTX [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 17}, BASE\\
\textbf{Operand binding:} ModR/M.r/m = [addr].

Save core thread context in standard format.

\subsection*{Operation}
\begin{lstlisting}
validate aligned standard context area
save core architectural context using the QUERY-reported revision
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{SBB -- SBB Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D9}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

Subtract source and borrow flag from destination.

\subsection*{Operation}
\begin{lstlisting}
borrow_in <- CF
difference <- unsigned(dst) - unsigned(src) - borrow_in
dst <- low_width_bits(difference)
update arithmetic FLAGS including borrow_out
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SC -- SC Rdst, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B9}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = [addr].

store-conditional (returns success).

\subsection*{Operation}
\begin{lstlisting}
if reservation still matches memory: atomic_store(memory, src); dst <- 1
else: memory unchanged; dst <- 0
clear reservation
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{SENDIPI -- SENDIPI target, vector, kind}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 0C}, SMP\\
\textbf{Operand binding:} ModR/M.reg = target; ModR/M.r/m = vector; XOP0 = kind.

Send an interprocessor message.

\subsection*{Operation}
\begin{lstlisting}
publish an interrupt-controller message of kind to target with vector
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{SETMODE -- SETMODE mode}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 1A}, BASE\\
\textbf{Operand binding:} immediate field = mode.

Serialized privileged operating-mode transition.

\subsection*{Operation}
\begin{lstlisting}
validate target mode, CR3, and required feature state
atomically enter target operating mode and serialize fetch
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{SFENCE -- SFENCE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{BC}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

store fence (ordering stores).

\subsection*{Operation}
\begin{lstlisting}
make all older stores globally visible before any younger store
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{SGT -- SGT Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{5B}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSGT Rdst,Rsrc (set if greater).

\subsection*{Operation}
\begin{lstlisting}
dst <- 1 if signed(dst) > signed(src) else 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHA1\_MSG1 -- SHA1\_MSG1 Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 08}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

SHA-1 message schedule step 1.

\subsection*{Operation}
\begin{lstlisting}
dst <- SHA-1 message schedule first transform on four 32-bit lanes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHA1\_MSG2 -- SHA1\_MSG2 Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 09}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

SHA-1 message schedule step 2.

\subsection*{Operation}
\begin{lstlisting}
dst <- SHA-1 message schedule second rotate/XOR transform on four 32-bit lanes
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHA256\_RNDS2 -- SHA256\_RNDS2 Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 0A}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

SHA-256 two-round helper.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHA256\_SIG0 -- SHA256\_SIG0 Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 0B}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

SHA-256 sigma0 helper.

\subsection*{Operation}
\begin{lstlisting}
for each 32-bit lane x: dst <- rotr(x,7) XOR rotr(x,18) XOR (x>>3)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHA256\_SIG1 -- SHA256\_SIG1 Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 0C}, X-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

SHA-256 sigma1 helper.

\subsection*{Operation}
\begin{lstlisting}
for each 32-bit lane x: dst <- rotr(x,17) XOR rotr(x,19) XOR (x>>10)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHL -- SHL Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{47}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSHL Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
count <- src mod width
dst <- trunc_width(dst << count)
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined for shifts; unchanged for rotates, AF=undefined, ZF=defined for shifts; unchanged for rotates, SF=defined for shifts; unchanged for rotates, OF=defined only for count 1.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHLX -- SHLX Rdst, Rsrc, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{CB}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; immediate field = imm.

uSHLX Rdst,Rsrc,imm.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(src << (immediate mod width)); FLAGS unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHR -- SHR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{48}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSHR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
count <- src mod width
dst <- unsigned(dst) >> count
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined for shifts; unchanged for rotates, AF=undefined, ZF=defined for shifts; unchanged for rotates, SF=defined for shifts; unchanged for rotates, OF=defined only for count 1.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SHRX -- SHRX Rdst, Rsrc, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{CC}, R-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc; immediate field = imm.

uSHRX Rdst,Rsrc,imm.

\subsection*{Operation}
\begin{lstlisting}
dst <- unsigned(src) >> (immediate mod width); FLAGS unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SLEEP -- SLEEP \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{8A}, S-type + imm\\
\textbf{Operand binding:} immediate field = \#imm.

uSLEEP \#imm (power mode).

\subsection*{Operation}
\begin{lstlisting}
request platform idle state immediate; return on interrupt or platform wake event
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{SLLV -- SLLV Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 16}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

variable left shift.

\subsection*{Operation}
\begin{lstlisting}
dst <- trunc_width(a << (b mod width))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SLT -- SLT Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{5A}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSLT Rdst,Rsrc (set if less than).

\subsection*{Operation}
\begin{lstlisting}
dst <- 1 if signed(dst) < signed(src) else 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SM4\_DEC -- SM4\_DEC Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 16}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

SM4 block decrypt.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SM4\_ENC -- SM4\_ENC Vd, Va}
\textbf{Status:} RESERVED\quad \textbf{Feature:} CRYPTO\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 02 15}, X-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

SM4 block encrypt.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SRAV -- SRAV Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 18}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

arithmetic right shift variable.

\subsection*{Operation}
\begin{lstlisting}
dst <- signed(a) >> (b mod width) with sign fill
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SRLV -- SRLV Rdst, Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 17}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra; XOP0 = Rb.

variable logical right shift.

\subsection*{Operation}
\begin{lstlisting}
dst <- unsigned(a) >> (b mod width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{ST -- ST [addr], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{11}, M-type + disp\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc.

uSTORE [addr],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
address <- effective_address(mem)
memory[address, operand_bytes] <- low_bits(src, operand_width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STB -- STB [addr], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{17}, M-type (1B)\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc.

uSTORE.B [addr],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
memory[effective_address(mem), 1] <- low_bits(src, 8)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STH -- STH [addr], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{18}, M-type (2B)\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc.

uSTORE.H [addr],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
memory[effective_address(mem), 2] <- low_bits(src, 16)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STI -- STI}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{86}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uSET\_IF (enable interrupts).

\subsection*{Operation}
\begin{lstlisting}
IF <- 1; maskable interrupts become eligible after the next instruction boundary
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STN -- STN [addr-], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{79}, M-type\\
\textbf{Operand binding:} ModR/M.r/m = [addr-]; ModR/M.reg = Rsrc.

uDEC addr; uSTORE [addr],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
new_address <- base - operand_bytes
memory[new_address, operand_bytes] <- src
base <- new_address after successful store
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STORE -- STORE}
\textbf{Status:} ALIAS\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{alias}, assembler alias\\
\textbf{Operand binding:} Inherited from canonical instruction.

Assembler alias for ST.

\subsection*{Operation}
\begin{lstlisting}
assemble as ST
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Inherited from canonical instruction.
\subsection*{Exceptions}
\begin{itemize}
\item Inherited from canonical instruction.
\end{itemize}
\clearpage

\section{STP -- STP [addr], Rsrc1, Rsrc2}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{1C}, M-type + pair\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc1; XOP0 = Rsrc2.

uSTORE [addr],Rsrc1; uSTORE [addr+sz],Rsrc2.

\subsection*{Operation}
\begin{lstlisting}
address <- effective_address(mem)
validate both stores before either becomes visible
memory[address] <- src1
memory[address + operand_bytes] <- src2
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STQ -- STQ [addr], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{1A}, M-type (8B)\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc.

uSTORE.Q [addr],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
memory[effective_address(mem), 8] <- low_bits(src, 64)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STW -- STW [addr], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{19}, M-type (4B)\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc.

uSTORE.W [addr],Rsrc.

\subsection*{Operation}
\begin{lstlisting}
memory[effective_address(mem), 4] <- low_bits(src, 32)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{STX -- STX [addr+], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{77}, M-type\\
\textbf{Operand binding:} ModR/M.r/m = [addr+]; ModR/M.reg = Rsrc.

uSTORE [addr],Rsrc; uINC addr.

\subsection*{Operation}
\begin{lstlisting}
old_address <- base
memory[old_address, operand_bytes] <- src
base <- old_address + operand_bytes after successful store
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{SUB -- SUB Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{22}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSUB Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst - src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SUBI -- SUBI Rdst, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{23}, I-type (1/2/4/8B)\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = \#imm.

uSUB Rdst,\#imm.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst - sign_extend(immediate), width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=defined, PF=defined, AF=defined, ZF=defined, SF=defined, OF=defined.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SUBS -- SUBS Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{32}, R-type (saturate)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSUB.SAT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- clamp_signed(signed(dst) - signed(src), width)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SUBU -- SUBU Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{33}, R-type (sat uns)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uSUB.USAT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- max(unsigned(dst) - unsigned(src), 0)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SUMDOTP -- SUMDOTP Rdst, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 13}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Va.

sum of dot-products helper.

\subsection*{Operation}
\begin{lstlisting}
dst <- left-to-right sum of signed adjacent-lane products within a
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SYSCALL -- SYSCALL}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{82}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uSYSCALL (trap to kernel).

\subsection*{Operation}
\begin{lstlisting}
create standard syscall frame on KSP0
CPL <- 0; FLAGS <- FLAGS & not SYSMASK
IP <- SYSENTRY
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{SYSRET -- SYSRET}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{83}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uSYSRET (return from kernel).

\subsection*{Operation}
\begin{lstlisting}
validate complete syscall frame
atomically restore user SP, IP, FLAGS, and CPL
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{TRAP -- TRAP \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{70}, S-type\\
\textbf{Operand binding:} immediate field = \#imm.

uTRAP imm (exception).

\subsection*{Operation}
\begin{lstlisting}
raise software interrupt vector immediate after saving a trap-class return frame
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{TST -- TST Ra, Rb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{56}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Ra; ModR/M.r/m = Rb.

uTST Ra,Rb (bitwise AND, set flags).

\subsection*{Operation}
\begin{lstlisting}
result <- a & b; discard result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{TSTI -- TSTI Ra, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{57}, I-type\\
\textbf{Operand binding:} ModR/M.r/m = Ra; ModR/M.reg = 0; immediate field = \#imm.

uTST Ra,\#imm (test bits).

\subsection*{Operation}
\begin{lstlisting}
result <- a & zero_extend(imm); discard result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{TZCNT -- TZCNT Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{C3}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uTZCNT Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- count_trailing_zeros(src, width); zero input returns width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{TZCNTV -- TZCNTV Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{CF}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uTZCNTV Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- count_trailing_zeros(src, width); zero input returns width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{TZCNT\_FAST -- TZCNT\_FAST Rdst, Ra}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 1B}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra.

fast count trailing zeros.

\subsection*{Operation}
\begin{lstlisting}
dst <- count_trailing_zeros(a, width); architecturally identical to CTZ
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{UDIV -- UDIV Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{2F}, R-type (unsigned)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uUDIV Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
if src == 0: raise DIV_ZERO
dst <- unsigned(dst) / unsigned(src), rounded down
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\end{itemize}
\clearpage

\section{UMUL -- UMUL Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{2E}, R-type (unsigned)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uUMUL Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
dst <- low_width_bits(unsigned(dst) * unsigned(src))
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{UMULH -- UMULH Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{DA}, R-type (unsigned high bits)\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

Return the high half of an unsigned full-width product.

\subsection*{Operation}
\begin{lstlisting}
product <- unsigned(dst) * unsigned(src) at twice operand width
dst <- high_width_bits(product)
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{UNPACK\_EXP -- UNPACK\_EXP Rdst, Ra}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} DSP\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 03 0F}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Ra.

unpack and expand elements.

\subsection*{Operation}
\begin{lstlisting}
dst lanes <- sign_extend corresponding packed source lanes to twice lane width
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VABS -- VABS Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A3}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

Per-lane: uABS Vd[i],Va[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- absolute_value(signed(a[i]))
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VADD -- VADD Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{97}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uADD Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] + b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VALIGN -- VALIGN Vd, Va, Vb, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 11}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; immediate field = imm.

align/join vectors.

\subsection*{Operation}
\begin{lstlisting}
result <- vector_bytes(concat(b,a), byte_offset, vector_bytes)
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VAND -- VAND Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{9B}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uAND Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] & b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VBLEND -- VBLEND Vd, Va, Vb, mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 06}, V-type+mask\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; immediate field = mask.

masked blend per-lane.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- b[i] when explicit mask bit i is one, otherwise a[i]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VBSWAP -- VBSWAP Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 12}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

byte-swap lanes per-lane.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- reverse_bytes_within_lane(a[i])
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VCOMPAND -- VCOMPAND Vd, Va, Vb}
\textbf{Status:} RESERVED\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 25}, V-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

conditional combine based on mask.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VCOMPARE\_EQ -- VCOMPARE\_EQ Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 2B}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; VectorCtl is required.

Per-lane predicate for a[i] == b[i].

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones if a[i] == b[i] else zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item SIMD\_ERROR for an invalid vector-control combination
\end{itemize}
\clearpage

\section{VCOMPARE\_GT -- VCOMPARE\_GT Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 0C}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane compare (gt).

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones if signed(a[i]) > signed(b[i]) else zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VCOMPARE\_LT -- VCOMPARE\_LT Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 0B}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane compare (lt).

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones if signed(a[i]) < signed(b[i]) else zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VCOMPARE\_NE -- VCOMPARE\_NE Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 2C}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; VectorCtl is required.

Per-lane predicate for a[i] != b[i].

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones if a[i] != b[i] else zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item SIMD\_ERROR for an invalid vector-control combination
\end{itemize}
\clearpage

\section{VCOMPARE\_UGT -- VCOMPARE\_UGT Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 2E}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; VectorCtl is required.

Per-lane predicate for unsigned(a[i]) $>$ unsigned(b[i]).

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones if unsigned(a[i]) > unsigned(b[i]) else zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item SIMD\_ERROR for an invalid vector-control combination
\end{itemize}
\clearpage

\section{VCOMPARE\_ULT -- VCOMPARE\_ULT Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 2D}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; VectorCtl is required.

Per-lane predicate for unsigned(a[i]) $<$ unsigned(b[i]).

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones if unsigned(a[i]) < unsigned(b[i]) else zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item SIMD\_ERROR for an invalid vector-control combination
\end{itemize}
\clearpage

\section{VCOMPRESS -- VCOMPRESS Vd, Va, mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 18}, V-type+mask\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = mask.

compress selected lanes.

\subsection*{Operation}
\begin{lstlisting}
pack lanes selected by mask into increasing low destination lanes; zero remaining lanes
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VCONVERT -- VCONVERT Vd, Va, format}
\textbf{Status:} RESERVED\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 27}, V-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

per-lane format conversions.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VDIV -- VDIV Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{9A}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uDIV Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] / b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item DIV\_ZERO or floating-point divide-by-zero before destination write
\end{itemize}
\clearpage

\section{VDUP -- VDUP Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A0}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

Broadcast: Vd[i]=Va[0] for all i.

\subsection*{Operation}
\begin{lstlisting}
for each destination lane i: result[i] <- source lane 0
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VEXPAND -- VEXPAND Vd, Va, mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 19}, V-type+mask\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = mask.

expand into sparse lanes.

\subsection*{Operation}
\begin{lstlisting}
consume packed low source lanes into destination positions selected by mask; zero other lanes
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VEXTRACT -- VEXTRACT Vd, Va, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 0E}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = imm.

extract lane/value.

\subsection*{Operation}
\begin{lstlisting}
result <- zero; result[0] <- a[lane_index]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VFMADD -- VFMADD Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 00}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane: fMA Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
result[i] <- correctly_rounded(d_old[i] + a[i] * b[i]) with one rounding
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{VFMADD\_ROUND -- VFMADD\_ROUND Vd,Va,Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 03}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

fused + rounding control.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- fused(d_old[i] + a[i] * b[i]) using encoded rounding mode
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{VFMADD\_SUB -- VFMADD\_SUB Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 1D}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

fused add/sub pattern.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- fused(d_old[i] + a[i]*b[i]) for even i and fused(d_old[i] - a[i]*b[i]) for odd i
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{VFMSUB -- VFMSUB Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 01}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane: fMS Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
result[i] <- correctly_rounded(d_old[i] - a[i] * b[i]) with one rounding
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{VFNMADD -- VFNMADD Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 02}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane: fNMA Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
result[i] <- correctly_rounded(d_old[i] - (a[i] * b[i])) with one rounding
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{VFPCLASS -- VFPCLASS Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 24}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

per-lane FP classification.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- IEEE classification bitmap for a[i]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item FPU\_ERROR or SIMD\_ERROR for an unmasked IEEE exception
\end{itemize}
\clearpage

\section{VGATHER -- VGATHER Vd, [base + idx*scale], mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 0F}, V-type+M\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = [base + idx*scale]; immediate field = mask.

gather with masking.

\subsection*{Operation}
\begin{lstlisting}
for each active lane i: address[i] <- base + signed(index[i]) * scale; element size <- lane_bytes
load active lanes in increasing order
on fault commit completed lanes and clear their mask bits; leave later lanes and mask bits unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Lane-ordered ordinary TSO accesses with the instruction's documented partial-progress state on fault.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VGATHERQ -- VGATHERQ Vd, [base+idx*scale], mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 22}, V-type+M\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = [base+idx*scale]; immediate field = mask.

64-bit gather variant.

\subsection*{Operation}
\begin{lstlisting}
for each active lane i: address[i] <- base + signed(index[i]) * scale; element size <- 8
load active lanes in increasing order
on fault commit completed lanes and clear their mask bits; leave later lanes and mask bits unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Lane-ordered ordinary TSO accesses with the instruction's documented partial-progress state on fault.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VINSERT -- VINSERT Vd, Va, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 0D}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = imm.

insert lane/value.

\subsection*{Operation}
\begin{lstlisting}
result <- d_old; result[lane_index] <- low lane_width bits of a
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VLD -- VLD Vd, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A1}, V-type + M\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = [addr].

Load vector from memory.

\subsection*{Operation}
\begin{lstlisting}
validate every active lane address before destination writeback
load active lanes in increasing lane order
apply merge/zero mask policy and commit destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VMASKMOV -- VMASKMOV Vd, Va, mask}
\textbf{Status:} RESERVED\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 15}, V-type+mask\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

masked store/load helpers.

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VMATRIX\_MUL -- VMATRIX\_MUL Vd, Va, Vb}
\textbf{Status:} RESERVED\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 26}, V-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

small-matrix fused ops (microcoded).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VMAX -- VMAX Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A4}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uMAX Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- max_signed(a[i], b[i])
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VMENTER -- VMENTER [vmcb]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} host\\
\textbf{Encoding:} \texttt{FF 04 0E}, VIRT\\
\textbf{Operand binding:} ModR/M.r/m = [vmcb].

Validate and enter a new guest.

\subsection*{Operation}
\begin{lstlisting}
validate complete VMCB and stage-2 root
save host state and atomically load guest state
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VMIN -- VMIN Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A5}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uMIN Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- min_signed(a[i], b[i])
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VMREAD -- VMREAD Rdst, field}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} host\\
\textbf{Encoding:} \texttt{FF 04 10}, VIRT\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0; immediate field = field.

Read an allowed VMCB field.

\subsection*{Operation}
\begin{lstlisting}
dst <- selected readable VMCB field
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{VMRESUME -- VMRESUME [vmcb]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} host\\
\textbf{Encoding:} \texttt{FF 04 0F}, VIRT\\
\textbf{Operand binding:} ModR/M.r/m = [vmcb].

Re-enter a previously exited guest.

\subsection*{Operation}
\begin{lstlisting}
validate resumable VMCB
save host state and atomically reload saved guest state
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VMUL -- VMUL Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{99}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uMUL Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] * b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VMULADDSUB -- VMULADDSUB Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 2A}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

fused multiply-add-sub across lanes.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- low_lane_bits(a[i]*b[i] + d_old[i]) for even i and low_lane_bits(a[i]*b[i] - d_old[i]) for odd i
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VMWRITE -- VMWRITE field, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} host\\
\textbf{Encoding:} \texttt{FF 04 11}, VIRT\\
\textbf{Operand binding:} ModR/M.mod = 11; ModR/M.reg = 0; ModR/M.r/m = Rsrc; trailing u16le field = field.

Write an allowed VMCB field.

\subsection*{Operation}
\begin{lstlisting}
validate field value and write selected writable VMCB field
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{VNOT -- VNOT Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 2F}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; VectorCtl is required.

Per-lane vector bitwise complement.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- bitwise_not(a[i])
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item SIMD\_ERROR for an invalid vector-control combination
\end{itemize}
\clearpage

\section{VOR -- VOR Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{9C}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uOR Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] | b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPACK -- VPACK Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 13}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

pack narrower elements.

\subsection*{Operation}
\begin{lstlisting}
result <- concatenate(saturating_narrow(a lanes), saturating_narrow(b lanes))
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPERM2 -- VPERM2 Vd, Va, Vb, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 17}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; immediate field = imm.

cross-lane permute/merge.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- concat(a,b)[(imm >> (i * ceil_log2(2*lane_count))) & (2*lane_count - 1)]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPERMUTE -- VPERMUTE Vd, Va, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 04}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = imm.

per-lane: permute lanes.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- a[(imm >> (i * ceil_log2(lane_count))) & (lane_count - 1)]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPMADD -- VPMADD Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 08}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane: pairwise multiply-add.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- a[2*i] * b[2*i] + a[2*i+1] * b[2*i+1]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPMAX -- VPMAX Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 20}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane integer max.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- max_signed(a[i], b[i])
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPMIN -- VPMIN Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 21}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane integer min.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- min_signed(a[i], b[i])
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VPMUL -- VPMUL Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 16}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

per-lane integer multiply.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- low_lane_bits(unsigned(a[i]) * unsigned(b[i]))
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VRECIP\_EST -- VRECIP\_EST Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 1B}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

reciprocal estimate per-lane.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- implementation estimate of 1/a[i] with relative error <= 2^-14 for normal finite inputs
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VREDUCE\_ADD -- VREDUCE\_ADD Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 09}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

reduction: sum lanes.

\subsection*{Operation}
\begin{lstlisting}
result[0] <- left-to-right modular sum of all active a lanes; other lanes zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VREDUCE\_MAX -- VREDUCE\_MAX Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 28}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

reduction: maximum value.

\subsection*{Operation}
\begin{lstlisting}
result[0] <- maximum active a lane; other lanes zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VREDUCE\_MIN -- VREDUCE\_MIN Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 29}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

reduction: minimum value.

\subsection*{Operation}
\begin{lstlisting}
result[0] <- minimum active a lane; other lanes zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VREDUCE\_MUL -- VREDUCE\_MUL Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 0A}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

reduction: product lanes.

\subsection*{Operation}
\begin{lstlisting}
result[0] <- left-to-right modular product of all active a lanes; other lanes zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VROUND -- VROUND Vd, Va, mode}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 1A}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = mode.

per-lane rounding control.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- round_integral(a[i], encoded_rounding_mode)
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VRSQRT\_EST -- VRSQRT\_EST Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 1C}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

reciprocal sqrt estimate per-lane.

\subsection*{Operation}
\begin{lstlisting}
result[i] <- implementation estimate of 1/sqrt(a[i]) with relative error <= 2^-14 for positive normal inputs
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VSCATTER -- VSCATTER [base + idx*scale], Vd, mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 10}, V-type+M\\
\textbf{Operand binding:} ModR/M.r/m = [base + idx*scale]; ModR/M.reg = Vd; immediate field = mask.

scatter with masking.

\subsection*{Operation}
\begin{lstlisting}
for each active lane i: address[i] <- base + signed(index[i]) * scale; element size <- lane_bytes
store active lanes in increasing order
on fault completed stores remain visible and completed mask bits are cleared
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Lane-ordered ordinary TSO accesses with the instruction's documented partial-progress state on fault.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VSCATTERQ -- VSCATTERQ [base+idx*scale], Vd, mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 23}, V-type+M\\
\textbf{Operand binding:} ModR/M.r/m = [base+idx*scale]; ModR/M.reg = Vd; immediate field = mask.

64-bit scatter variant.

\subsection*{Operation}
\begin{lstlisting}
for each active lane i: address[i] <- base + signed(index[i]) * scale; element size <- 8
store active lanes in increasing order
on fault completed stores remain visible and completed mask bits are cleared
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Lane-ordered ordinary TSO accesses with the instruction's documented partial-progress state on fault.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VSHL -- VSHL Vd, Va, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{9E}, V-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = \#imm.

Per-lane: uSHL Vd[i],Va[i],imm.

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- trunc_lane(a[i] << immediate)
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VSHR -- VSHR Vd, Va, \#imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{9F}, V-type + imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; immediate field = \#imm.

Per-lane: uSHR Vd[i],Va[i],imm.

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- unsigned(a[i]) >> immediate
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VSHUFFLE -- VSHUFFLE Vd, Va, Vb, imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 05}, V-type+imm\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb; immediate field = imm.

per-lane: shuffle bytes/lanes.

\subsection*{Operation}
\begin{lstlisting}
result byte i <- concat(a,b)[(imm >> (i * ceil_log2(2*vector_bytes))) & (2*vector_bytes - 1)]
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VST -- VST [addr], Vd}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{A2}, V-type + M\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Vd.

Store vector to memory.

\subsection*{Operation}
\begin{lstlisting}
validate every active lane address before any store becomes visible
store active lanes in increasing lane order as one precise instruction
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Ordinary TSO access unless the instruction entry states atomic or device ordering.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{VSUB -- VSUB Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{98}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uSUB Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] - b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VTEST -- VTEST Vd, Va}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 07}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va.

predicate/test (set mask).

\subsection*{Operation}
\begin{lstlisting}
result[i] <- all_ones when (d_old[i] & a[i]) != 0, otherwise zero
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VUNPACK -- VUNPACK Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 14}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

unpack to wider lanes.

\subsection*{Operation}
\begin{lstlisting}
result even lanes <- zero_extend(a low half lanes); odd lanes <- zero_extend(b low half lanes)
apply encoded merge/zero mask policy
commit complete vector destination atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VXOR -- VXOR Vd, Va, Vb}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SIMD\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{9D}, V-type\\
\textbf{Operand binding:} ModR/M.reg = Vd; ModR/M.r/m = Va; XOP0 = Vb.

Per-lane: uXOR Vd[i],Va[i],Vb[i].

\subsection*{Operation}
\begin{lstlisting}
for each active lane i:
    result[i] <- a[i] ^ b[i]
apply encoded merge/zero mask policy
commit destination vector atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VZEROALL -- VZEROALL}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 1F}, V-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

zero all vector state registers.

\subsection*{Operation}
\begin{lstlisting}
clear all enabled bits of V0-V31 and K0-K7
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{VZEROUPPER -- VZEROUPPER}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} AVX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 01 1E}, V-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

zero upper vector state (control).

\subsection*{Operation}
\begin{lstlisting}
for every vector register: clear bits 128 through enabled_vector_width-1
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{WFI -- WFI}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{87}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uWAIT\_FOR\_INTERRUPT.

\subsection*{Operation}
\begin{lstlisting}
wait until an eligible interrupt or implementation event; architectural state is unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{WINNEW -- WINNEW}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} WINDOW\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 1B}, X-type (no operands)\\
\textbf{Operand binding:} No ModR/M, XOP, immediate, or displacement bytes.

Advance to a fresh logical register window without control transfer.

\subsection*{Operation}
\begin{lstlisting}
validate window state and spill-area configuration
create a child window; map caller R24-R31 to child R8-R15
initialize child R16-R31 to zero
make the child window current atomically
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
May perform transparent window spill or restore memory accesses; those accesses are ordered as ordinary core-context loads/stores and complete before retirement.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable WINDOW feature
\item GPF when CR4.WINDOW\_ENABLE is clear
\item GPF for invalid or exhausted WSP configuration
\item ALIGN\_CHECK or PAGE\_FAULT before architectural modification if a transparent spill/restore fails
\end{itemize}
\clearpage

\section{WINPIN -- WINPIN}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} WINDOW\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 1E}, X-type (no operands)\\
\textbf{Operand binding:} No ModR/M, XOP, immediate, or displacement bytes.

Request advisory retention of the current logical window.

\subsection*{Operation}
\begin{lstlisting}
increment the current window retention hint count
do not prevent a spill required for correctness or forward progress
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable WINDOW feature
\item GPF when CR4.WINDOW\_ENABLE is clear
\end{itemize}
\clearpage

\section{WINPREV -- WINPREV}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} WINDOW\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 1C}, X-type (no operands)\\
\textbf{Operand binding:} No ModR/M, XOP, immediate, or displacement bytes.

Restore the previous logical register window without returning.

\subsection*{Operation}
\begin{lstlisting}
require a previous logical window
propagate child R8-R15 through the overlap to parent R24-R31
make the parent window current atomically; restore it transparently if spilled
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
May perform transparent window spill or restore memory accesses; those accesses are ordered as ordinary core-context loads/stores and complete before retirement.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable WINDOW feature
\item GPF when CR4.WINDOW\_ENABLE is clear
\item GPF for window underflow or invalid WSP configuration
\item ALIGN\_CHECK or PAGE\_FAULT before architectural modification if a transparent spill/restore fails
\end{itemize}
\clearpage

\section{WINRELEASE -- WINRELEASE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} WINDOW\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 1F}, X-type (no operands)\\
\textbf{Operand binding:} No ModR/M, XOP, immediate, or displacement bytes.

Release one advisory window-retention request.

\subsection*{Operation}
\begin{lstlisting}
require a nonzero retention hint count
decrement the current window retention hint count
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable WINDOW feature
\item GPF when CR4.WINDOW\_ENABLE is clear
\item GPF for an unbalanced release
\end{itemize}
\clearpage

\section{WINRESERVE -- WINRESERVE}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} WINDOW\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 1D}, X-type (no operands)\\
\textbf{Operand binding:} No ModR/M, XOP, immediate, or displacement bytes.

Hint that another logical window will be needed soon.

\subsection*{Operation}
\begin{lstlisting}
optionally prepare microarchitectural resident capacity
do not spill, fault, or change architectural window state
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable WINDOW feature
\item GPF when CR4.WINDOW\_ENABLE is clear
\end{itemize}
\clearpage

\section{WRCR -- WRCR CRn, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{81}, S-type\\
\textbf{Operand binding:} ModR/M.mod = 11; ModR/M.reg = 0; ModR/M.r/m = Rsrc; OREX extends only Rsrc; trailing u16le field = CRn; canonical length = 4 bytes, or 7 bytes with FE/OREX.

uWRITE\_CTRL\_REG CRn,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
validate register, value, privilege, and reserved bits
commit system_register[CRn] <- src as one serializing operation
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\end{itemize}
\clearpage

\section{WRSS -- WRSS [addr], Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} kernel\\
\textbf{Encoding:} \texttt{FF 04 13}, SHSTK\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = Rsrc.

Privileged shadow-stack write.

\subsection*{Operation}
\begin{lstlisting}
validate shadow-stack mapping
memory[effective_address(mem), pointer_bytes] <- src using shadow-stack write permission
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF when current privilege is insufficient
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{XABORT -- XABORT Rcode}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D4}, S-type\\
\textbf{Operand binding:} ModR/M.r/m = Rcode; ModR/M.reg = 0.

Abort transaction with register code.

\subsection*{Operation}
\begin{lstlisting}
set explicit-abort reason and low eight-bit user code
restore checkpoint and branch to fallback
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XABORT -- XABORT imm}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D3}, S-type+imm\\
\textbf{Operand binding:} immediate field = imm.

Abort transaction with immediate code.

\subsection*{Operation}
\begin{lstlisting}
set explicit-abort reason and low eight-bit user code
restore checkpoint and branch to fallback
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XBEGIN -- XBEGIN label}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D0}, B-type (rel32)\\
\textbf{Operand binding:} immediate field = label.

Begin transactional region with a signed PC-relative abort target.

\subsection*{Operation}
\begin{lstlisting}
checkpoint architectural state
set transaction fallback to next_IP + sign_extend(displacement)
enter transactional execution
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XBEGINA -- XBEGINA Rtarget}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D1}, B-type (abs)\\
\textbf{Operand binding:} ModR/M.r/m = Rtarget; ModR/M.reg = 0.

Begin transactional region with an absolute abort target register.

\subsection*{Operation}
\begin{lstlisting}
checkpoint architectural state
set transaction fallback to absolute target
enter transactional execution
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XCHG -- XCHG Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{1F}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uXCHG Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
atomically exchange the two register operands, or perform one acquire+release read-modify-write when either operand is memory
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{XCHG -- XCHG Rvalue, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{B1}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Rvalue; ModR/M.r/m = [addr].

atomically exchange Rvalue with memory and return the old memory value.

\subsection*{Operation}
\begin{lstlisting}
atomically exchange the two register operands, or perform one acquire+release read-modify-write when either operand is memory
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{XCHG128 -- XCHG128 Vvalue, [addr]}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} ATOMICS\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{BF}, M-type\\
\textbf{Operand binding:} ModR/M.reg = Vvalue; ModR/M.r/m = [addr].

atomically exchange a 128-bit vector value with aligned memory (Droplet mode).

\subsection*{Operation}
\begin{lstlisting}
require 16-byte alignment and ATOMIC128 feature
atomically exchange 128-bit register value and memory operand
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Atomic read-modify-write with acquire+release ordering; .SC form is sequentially consistent.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{XEND -- XEND}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D2}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

End transactional region (commit).

\subsection*{Operation}
\begin{lstlisting}
if not in transaction: raise INVALID_OP
attempt atomic commit; on failure abort to fallback
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XNOR -- XNOR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{46}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uXNOR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst XNOR src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XOR -- XOR Rdst, Rsrc}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{42}, R-type\\
\textbf{Operand binding:} ModR/M.reg = Rdst; ModR/M.r/m = Rsrc.

uXOR Rdst,Rsrc.

\subsection*{Operation}
\begin{lstlisting}
width <- encoded operand width
result <- trunc_width(dst ^ src, width)
dst <- result
\end{lstlisting}
\subsection*{Flags}
CF=cleared, PF=defined, AF=undefined, ZF=defined, SF=defined, OF=cleared.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XRESUME -- XRESUME}
\textbf{Status:} RESERVED\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D6}, S-type\\
\textbf{Operand binding:} Reserved encoding; no operand bytes are decoded.

Resume speculative transactional state (helper).

\subsection*{Operation}
\begin{lstlisting}
reserved v2 allocation; execution raises INVALID_OP
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XRSTOR -- XRSTOR [addr], mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 04}, XSTATE\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = mask.

Validate and restore selected extended state.

\subsection*{Operation}
\begin{lstlisting}
validate the entire requested image before state change
atomically restore requested enabled components
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{XSAVE -- XSAVE [addr], mask}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} SYSX\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{FF 04 03}, XSTATE\\
\textbf{Operand binding:} ModR/M.r/m = [addr]; ModR/M.reg = mask.

Save selected extended state.

\subsection*{Operation}
\begin{lstlisting}
validate 64-byte aligned save area and component mask
write requested enabled components at QUERY-reported offsets
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
Architectural multi-byte memory access with the validation and atomicity contract stated by the instruction.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\item GPF for non-canonical address
\item ALIGN\_CHECK when required alignment is violated
\item PAGE\_FAULT for translation or permission failure
\end{itemize}
\clearpage

\section{XSTATUS -- XSTATUS Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D7}, R-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

Query transaction status / reason code.

\subsection*{Operation}
\begin{lstlisting}
dst <- last transaction status bitfield; reading clears no bits
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{XTEST -- XTEST Rdst}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} TXN\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{D5}, R-type\\
\textbf{Operand binding:} ModR/M.r/m = Rdst; ModR/M.reg = 0.

Test transactional state (in-txn?).

\subsection*{Operation}
\begin{lstlisting}
dst <- 1 if transaction active else 0
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\section{YIELD -- YIELD}
\textbf{Status:} NORMATIVE\quad \textbf{Feature:} BASE\quad \textbf{Privilege:} user\\
\textbf{Encoding:} \texttt{71}, S-type\\
\textbf{Operand binding:} No ModR/M or XOP byte.

uYIELD (scheduler hint).

\subsection*{Operation}
\begin{lstlisting}
record scheduler hint; architectural state is otherwise unchanged
\end{lstlisting}
\subsection*{Flags}
CF=unchanged, PF=unchanged, AF=unchanged, ZF=unchanged, SF=unchanged, OF=unchanged.
\subsection*{Memory Ordering}
No memory access.
\subsection*{Exceptions}
\begin{itemize}
\item INVALID\_OP for malformed encoding or unavailable feature
\end{itemize}
\clearpage

\end{document}
